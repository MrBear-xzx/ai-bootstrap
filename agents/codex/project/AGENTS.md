# <项目名称>

<一句话说明项目用途；技术栈能够从项目文件明确识别时，无需重复罗列>

---

> **模板使用说明**
>
> 复制到项目根目录后，根据实际项目填写，并删除本说明、占位符以及不适用的章节。
> 已在用户级 `AGENTS.md` 中定义的通用规则无需在此重复。

## 项目结构 [选填]

- `<目录>`：<职责或边界>
- `<目录>`：<职责或边界>
- <仅说明不容易从目录名称和代码中判断的结构>

## 开发与验证命令

```bash
# 安装依赖
<命令>

# 启动开发环境
<命令>

# 运行相关测试
<命令>

# 完整验证
<命令>

# Lint / 类型检查
<命令>
```

- 使用 `<npm / pnpm / yarn / Maven / Gradle 等>` 作为本项目的标准工具。
- 仅保留本项目实际存在并推荐使用的命令。
- 如果某类命令不适用，删除对应内容。

## 完成标准

完成代码修改前，应根据修改范围执行：

- <受影响模块的测试要求>
- <Lint、类型检查或构建要求>
- <数据库迁移、接口文档、生成文件等同步要求>
- 无法执行必要验证时，必须说明未执行的项目及原因。

## 代码与架构约束 [选填]

- <项目特有且无法由 formatter、linter 或类型系统自动保证的约定>
- <模块依赖方向或架构边界>
- <必须复用的公共组件、工具或抽象>
- <禁止引入的实现方式>

## 文件与目录限制 [选填]

- 不要直接修改 `<生成代码目录>`。
- 不要修改 `<第三方代码或锁定目录>`。
- 修改 `<源文件>` 后，通过 `<命令>` 重新生成对应文件。
- <其他不可修改文件或特殊目录>

## 项目注意事项 [选填]

- <非显而易见的业务规则>
- <常见陷阱或历史兼容要求>
- <容易误用的旧模块、旧接口或废弃实现>
- <本地环境与 CI 行为之间的重要差异>

## 子目录规则 [选填]

当某个模块存在独立规则时，在对应目录中创建更具体的 `AGENTS.md` 或 `AGENTS.override.md`，不要继续扩充根目录文件。
